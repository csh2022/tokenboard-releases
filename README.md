# TokenBoard CLI Releases

This repository hosts public binary releases for the TokenBoard CLI.

The TokenBoard source code is maintained in a separate private repository. This
repository is intentionally limited to release metadata and downloadable binary
assets.

Install TokenBoard from the product website:

```bash
curl -fsSL https://token-board.com/install.sh | sh
```
